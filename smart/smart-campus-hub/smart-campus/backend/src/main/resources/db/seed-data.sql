-- Smart Campus Hub seed data
-- Tables are created by Hibernate (ddl-auto=update).
-- Initial users and facilities are seeded by DataInitializer.java (CommandLineRunner).
-- Place any additional INSERT statements here; continue-on-error=true is enabled.
SELECT 1;
